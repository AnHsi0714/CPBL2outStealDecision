# 兩出局一壘有人盜壘決策：分析發現

補充 `兩出局一壘有人盜壘決策_專案計畫書.md`「四、工作分解與時程」各項分析的完整數字與結論。計畫書只保留任務描述與檢查點，詳細發現整理於此，方便查閱與引用；產生報告的方法見 CLAUDE.md「分析管線」與 README「分析管線」。

## 基礎門檻

- 全季損益兩平門檻中位數 58.5%（整體平均法 58.6%；修正逐球資料公告列污染前為 58.1%，見下方「引擎驗證」）
- 逐棒次（1–9）門檻分解：超出原計畫範圍的加碼分析，發現第 4、9 棒因不同機制各自成為局部高點

## TTO（三振＋全壘打＋保送）複合指標

`pipeline/model_batter_decisions.py`／`pipeline/analyze_batter_types.py` 算出 `TTO_proxy` 並依中位數分組。結果：高 TTO 組門檻中位數 62.3% vs 低 TTO 組 55.5%（+6.8pp，p<0.001），方向與長打/選球分組一致；TTO 與 ISO 相關係數 0.44（非重複指標）。

## 各隊決策品質評估

`analysis/analyze_team_decisions.py` 用二項檢定（binomtest）比對各隊實際成功率與自家門檻中位數，p<0.05 才判「跑對／跑錯」，否則標「無法判定」。只判跑對/跑錯，不判「該跑不跑」，因樣本量（27–49次）連前者的顯著性都撐得勉強（計畫書 3.3 節已提醒此類切層樣本不足）。結果：六隊僅統一7-ELEVEn獅顯著跑對（實際成功率 79.2% vs 門檻 58.9%，p=0.005），其餘五隊皆無法判定，不能直接說是跑錯。

## 符合門檻的跑者名單

`analysis/analyze_runner_steal_rates.py` 逐球掃描全季快取，用 `FirstBase` 回推真實 `HitterAcnt`，只用當季資料不跨季合併（球員可能轉型或老化，教練該看這一季的實際表現）。**判定刻意不比對單一門檻**：門檻本身隨下一棒棒次而變，把跑者成功率拿去比對單一全季中位數等於丟掉這個結論。改成逐棒次比對，每位跑者分「任一棒次都可跑／視下一棒棒次而定／任一棒次都不建議跑／樣本不足（嘗試<5次）」四級。2025 年：103 位跑者中 24 位任一棒次都可跑、8 位視棒次而定、16 位任一棒次都不建議跑、55 位樣本不足。結果併入決策報告頁面（可搜尋／篩選）。

## 左右投對跑者的影響

`analysis/analyze_pitcher_handedness.py` 只回答一件事：**投手慣用手如何影響跑者盜二壘的實際成功率**，不涉及打者、不影響門檻本身（門檻完全由打者的打擊結果分布決定，模型裡沒有用到投手）。加 `--pool-years 2023,2024,2025,2026` 合併四季做逐跑者拆分（單季拆完多數跑者樣本不足）。

四季結果：

| 球季 | 對左投成功率 (n) | 對右投成功率 (n) | 差距 | p |
|---|---|---|---|---|
| 2023 | 60.2% (103) | 71.3% (432) | +11.1pp | 0.033 |
| 2024 | 66.5% (164) | 70.4% (419) | +3.9pp | 0.370 |
| 2025 | 58.6% (222) | 69.9% (459) | +11.4pp | 0.004 |
| 2026 | 62.9% (89) | 69.7% (340) | +6.8pp | 0.250 |
| **四季合計** | **61.8% (578)** | **70.4% (1650)** | **+8.6pp** | **0.00018** |

方向四季一致，合併後高度顯著；個別球季只有 2023、2025 顯著，因為每季每手別只有 89–222 次嘗試。**跑者幾乎沒有因應調整**：每球嘗試率對左投 1.57%、對右投 1.73%（p=0.038），只收斂約 9%，遠不足以抵銷 8.6pp 的成功率落差。對照門檻（55–59%）：對右投跑 70.4% 遠高於門檻，划算；對左投跑 61.8% 幾乎卡在門檻上，等於白忙。

合併四季、左右投各 ≥5 次嘗試的 36 位跑者中，24 位對左右投都可跑、**11 位只建議對右投跑**（對右投過門檻、對左投不過，如李凱威 52.4% vs 73.8%、王博玄 53.3% vs 79.1%）、1 位都不建議。個別差異很大：邱智呈反而是對左投更好（88.2% vs 67.7%），不能一律假設左投必然不利。

附帶觀察：牽制投球率在 2026 明顯下降（左投 4.17%／右投 4.98%，前三季為 6.0–8.0%），與 2026 牽制次數限制新規的方向一致。

**左右投：必須分清楚的兩件事**（詳見 CLAUDE.md 同名章節）：
- 投手慣用手影響的是「跑不跑得掉」（實際成功率），不是門檻；把現有結果按投手左右投分兩堆比較，門檻會幾乎一樣，那是算法造成的，不是真的沒差異。
- 若真要讓門檻隨投手變，須把打者成績依對戰關係（打者手×投手手）分開建檔，而非投手手別本身；此路因樣本不足（四季只有 17/22/15/0 位打者達標 100 個對左投打席）目前未採用。

## RE24 矩陣

`re24/build_re24_matrix.py`：2025 年 360 場、27,699 個 state 區段，見 `outputs/cpbl_re24_matrix_2025_A_1-360.csv`。視覺化：`re24/generate_re24_report.py` 產生 `reports/cpbl-re24-matrix-2025.html`。

## 跨年度穩定性檢查（2023–2026 四季）

改以 CPBL 官網爬蟲補齊 2023–2026 四季（未用野球革命 Open Data），同一管線、均為公告列污染修正後重跑，可直接比較。合計 1,254 場、6,506 筆樣本。

- **站得住**：低點固定在第 1、2 棒；第 4 棒為高點（純能力門檻四季都最高：65.4／61.9／65.9／57.8%）；單打率分組四季皆極顯著且效果量最大（4.9–7.3pp）；ISO 分組四季皆顯著
- **保留效應四季一致**：逐棒次保留貢獻第 1–5 棒為負、第 6 棒後翻正、第 8 棒四季都最嚴重（+3.1～4.1pp，`analysis/analyze_retention_contribution.py` 用 `ModelVIfBatterOut` 反事實重算，不必重跑模擬）。機制是「打線多墊一棒才輪到棒頭」，非原先設想的「賠掉第1棒開路」
- **站不住**：「第 9 棒門檻最高」不成立（3 季最高點在第 4 棒、1 季在第 3 棒）：第 9 棒的能力效應往下拉、保留效應往上推，兩者相抵
- **無鑑別力**：前段(1-5) vs 後段(6-9) 二分法四季 p=0.098–0.55，全部不顯著，應改用逐棒次分解
- **需修正強度**：長打／選球型態仍是最乾淨的切法，但打擊率、出局率其實跟門檻也有中等強度、方向明確的相關（打擊率愈高門檻愈低、出局率愈高門檻愈高），並非「幾乎無關」（`analysis/analyze_batter_threshold_correlations.py`，門檻取該打者所有決策點中位數）
- **待確認**：選球力（BB%）分組 2026 不顯著（p=0.075，前三季皆極顯著）
- **結構斷點**：2023 五隊 300 場制、2024 起六隊 360 場制（台鋼加入），不可解讀為戰術演變；2026 僅至第 240 場、球季進行中

## 失效條件一：比分落後時門檻應上調

`analysis/analyze_score_diff_steal_rate.py` 量出盜壘嘗試率隨分差驟降：平手/小分差時 12.8%–22.1%，±5–6 分以上掉到 3–4%（占全體樣本 13.7%），比原訂猜測的「3 分」更寬。`analysis/analyze_score_diff_threshold_sensitivity.py` 確認排除這類極端分差樣本後，門檻中位數變動四季皆在 0.2pp 內（2023 例外，最大到 +0.63pp），對結論無實質影響，不需從主管線剔除，文字揭露即可。**但這只證明「極端分差樣本沒有拉偏整體門檻」，不證明「該情境下門檻本身不需調整」**：RE 本質上不看比分情境，真正回答「該不該調、調多少」需要改用勝率增值（WPA），見下一項。

## 失效條件二：第 7–8 局應改用 WPA 而非 RE 當判準

與上一項是同一個方法論限制（RE 不含比分/局數情境）的兩種呈現。`win_expectancy/build_win_expectancy_matrix.py` 從四季合併的逐球紀錄算出「局數×攻守方×分差×出局×壘包→進攻方最終獲勝機率」的經驗勝率表（局數桶 1–6/7/8/9+、分差上限 ±5，共 2112 格，96,814 個 state 區段觀測值），`win_expectancy/validate_win_expectancy_matrix.py` 做單調性與目標情境涵蓋度檢查，`win_expectancy/generate_we_report.py` 產出互動熱力圖（`reports/cpbl-win-expectancy-matrix.html`）；`wpa/model_wpa_decisions.py` 把這張表接回三分支模型算出 WPA 版損益兩平門檻，並跟 RE 版逐局對照，`wpa/bootstrap_wpa_threshold_ci.py` 補上 95% 信賴區間驗證結論站不站得住。

逐局把 WPA 門檻 95% CI 上界跟同局 RE 門檻中位數比對，四季結果一致：第 2–8 局 CI 上界完全碰不到 RE 門檻（0/4 季重疊），只有第 1 局在 2 個季重疊：第 1 局分母（V成功－V失敗）中位數僅 0.04、跟其標準誤同量級，比值結構性不穩定，不是樣本不夠。**結論：第 2–8 局改用 WPA 當判準的方向都站得住，不是只有第 7–8 局**；第 6/7/8 局 95% CI 四季分別落在約 24–44%／19–32%／21–33%，完全在 RE 版 54–60% 之下。但「點估計」本身的穩定性要分兩段看：第 2–3 局的中位數落在自己 95% CI 之外（四季皆然，跟第 1 局同一種分母不穩問題，只是沒那麼嚴重），只有方向可信、精確數字不可信；第 4 局起中位數才穩定落在自己的 CI 之內（四季皆然），此後數字才適合直接引用（例如第 7–8 局門檻約 20–33%，RE 版約 54–60%）。另外第 7–8 局的 V成功查表仍有 44–55% 退化到忽略局數桶的合併值（理論上是稀釋效應、不會誇大結論）。

機制：同一分的份量隨局數變重，攻方領先 1 分時的勝率，第 1–6 局約 59%、第 7 局 72%、第 8 局 79%（落後 1 分則反向放大），讓半局現在結束的代價隨局數上升，壓縮門檻公式的分子。`wpa/analyze_wpa_runner_reclassification.py` 把這件事接回實際球員名單：RE 判「任一棒次都不建議跑」的跑者，四季分別有 100%／67%／88%／50%（2024、2026 名單只有 2、3 人，波動屬小樣本正常現象）在第 4–8 局至少一局的 WPA 門檻下其實夠格盜壘，方向四季一致。這代表 RE 版判準不只低估晚局門檻該多低，也低估了現有跑者裡有多少人夠格在晚局盜壘。

`wpa/compare_wpa_groups.py` 進一步把「棒次 × 打者類型分組分析」的既有結論換成 WPA（只用第 4-8 局，RE 版也用同一子集重算，公平比較）：棒次分析「低點在 1、2 棒」在此切法下四季不穩定，暫時未決；長打力／選球力分組方向與 RE 版大致同調；單打率／TTO 分組方向不變但統計顯著性消失（樣本切細撐不住，非效應消失）；**真上壘率（OBP）分組方向四季一致反轉**（RE：高 OBP 門檻較低；WPA：高 OBP 門檻較高，3/4 季顯著），是唯一真正翻盤的分組，機制待進一步拆解。

## 引擎驗證

`re24/validate_re24_simulation.py` 逐格比較模擬 RE24 與真實 RE24：24 格相關係數 r=0.992、加權平均誤差 0.033 分，最大誤差都落在稀有壘包組合（樣本數<400）。過程中抓到一個真實資料品質 bug：逐球資料的換投手/代打/代跑/守備公告列殘留舊出局數，誤污染約 3% 的列，集中灌高「2出局、空壘」這格。已修正並重跑全部下游腳本。

## 盜壘文字判讀對帳

`analysis/validate_steal_parsing.py` 把 `is_steal_success`／`is_steal_failure` 的文字判讀邏輯套用到全場（不限兩出局、一壘有人），逐場逐隊加總比對 CPBL 官方 box score 的 `StealBaseOKCnt`／`StealBaseFailCnt`：2025 年 360 場，720 個（場次,球隊）組合，盜壘成功 508/508、盜壘刺 242/242，完全相符。過程中修正兩個文字判讀邊界案例：盜壘刺但因野手失誤反而安全上壘；原住民選手全形間隔號名字過長導致配對失敗。

## 敏感度分析與 Bootstrap 信賴區間

`analysis/bootstrap_threshold_ci.py`（case resampling ＋ 用既有 `ModelV*SE` 加模擬雜訊，不必重跑模擬）＋ `analysis/analyze_hyperparameter_sensitivity.py`（模擬次數 500/1000/2000/4000、`prior_pa` 25/50/100、`minimum_transition_cell` 3/5/10、`min-pa` 50/100/150，四季都做）。

結果：模擬次數 2000 已收斂（500 次偏離最多 −1.6pp、4000 次 <0.7pp）；`prior_pa`／`minimum_transition_cell`／`min-pa` 對 ISO／BB%／單打率／TTO 的結論都穩健，只有 2025 年 `minimum_transition_cell=10` 是例外（+3.16pp，稀疏格 `(2出局,一壘有人,三壘安打)` 只有 5 筆樣本剛好壓在門檻上，屬資料量脆弱點非程式錯誤）；**OBP 分組的顯著性判定本身不穩健**：bootstrap CI 四季都包含 0、`min-pa` 三季會翻轉顯著性，跟既有「方向一致但弱」的定性一致，簡報應只講方向不講顯著性。
